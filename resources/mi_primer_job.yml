# resources/mi_primer_job.yml
resources:
  jobs:
    mi_primer_job_bundle:
      name: "Mi Primer Job Desplegado con un Bundle"
      #buscar si puedo parametrizar un id para el job
      #id: 
      tasks:
        - task_key: tarea_notebook
          
          # Le decimos al job que use un clúster que ya existe.
          existing_cluster_id: "0604-154954-8pbp3p4r"


          notebook_task:
            # La ruta al notebook es relativa.
            notebook_path: ../src/databricksbundledemo/mi_primer_notebook_bundle.py